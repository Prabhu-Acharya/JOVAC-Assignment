# JOVAC HTML Assignments

## 📁 image-tag-assignment
- Shows how to use `<img>` tag with `src`, `alt`, `width`, and `height`.

## 📁 anchor-tag-assignment
- Demonstrates usage of `<a>` tag with `href`, `title`, and `target="_blank"`.
